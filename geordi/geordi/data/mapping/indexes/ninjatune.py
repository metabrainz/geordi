from ..rule import Rule
ninjatune = {
    'release': [Rule(['PRODUCT TITLE', ('index', True)], ['release', 'title']),
                Rule(['Product Title', ('index', True)], ['release', 'title'])]
}
